{
  "name": "citrea-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Citrea zkEVM app",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:polygon-zk": "hardhat run scripts/deploy.js --network polygonZkEvmTestnet",
    "deploy:scroll": "hardhat run scripts/deploy.js --network scrollTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.0.3"
  }
} 